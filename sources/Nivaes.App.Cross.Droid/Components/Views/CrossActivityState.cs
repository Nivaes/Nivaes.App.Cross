namespace Nivaes.App.Cross.Droid
{
    public enum CrossActivityState
    {
        OnCreate,
        OnRestart,
        OnStart,
        OnResume,
        OnPause,
        OnStop,
        OnDestroy,
        OnSaveInstanceState,
        OnNewIntent
    }
}
