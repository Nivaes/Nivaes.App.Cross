namespace Nivaes.App.Cross.Droid
{
    public enum MvxActivityState
    {
        OnCreate,
        OnRestart,
        OnStart,
        OnResume,
        OnPause,
        OnStop,
        OnDestroy,
        OnSaveInstanceState,
        OnNewIntent
    }
}
