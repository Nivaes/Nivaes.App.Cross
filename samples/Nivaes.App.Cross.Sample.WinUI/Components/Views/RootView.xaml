<?xml version="1.0" encoding="utf-8"?>
<app:RootViewPage
    x:Class="Nivaes.App.Cross.Sample.WinUI.RootView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nivaes.App.Cross.Sample.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:app="using:Nivaes.App.Cross.Sample.WinUI" 
    xmlns:sample="using:Nivaes.App.Cross.Sample" 
    xmlns:converters="using:Nivaes.App.Cross.WinUI"
    d:DataContext="{d:DesignInstance Type=sample:RootViewModel}"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:DebugValueConverter x:Key="DebugValueConverter" />
        <local:RootViewValueConverter x:Key="RootViewValueConverter" />
    </Page.Resources>

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="Binding Title" />
        <TextBlock Text="{x:Bind ViewModel.Title, Mode=OneWay}" />

        <TextBlock Text="Binding Title" />
        <TextBlock Text="{Binding Title, Mode=OneWay}" />        

        <Button x:Name="myButton" Click="myButton_Click">Click Me view</Button>
    </StackPanel>
</app:RootViewPage>
