<?xml version="1.0" encoding="utf-8"?>
<app:RootViewPage
    x:Class="Nivaes.App.Cross.Sample.WinUI3.RootView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nivaes.App.Cross.Sample.WinUI3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:app="using:Nivaes.App.Cross.Sample.WinUI3" 
    xmlns:sample="using:Nivaes.App.Cross.Sample" 
    xmlns:converters="using:Nivaes.App.Cross.WinUI3"
    d:DataContext="{d:DesignInstance Type=sample:RootViewModel}"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:CrossDebugValueConverter x:Key="CrossDebugValueConverter" />
        <local:RootViewValueConverter x:Key="RootViewValueConverter" />
    </Page.Resources>

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

        <!--<Button Click="{Binding ShowNewWindowCommand}">Show new window</Button>
        <Button Click="{Binding FormCommand}">Show Form</Button>
        <Button Click="{Binding SubFormCommand}">Show SubForm</Button>-->
    </StackPanel>
</app:RootViewPage>
