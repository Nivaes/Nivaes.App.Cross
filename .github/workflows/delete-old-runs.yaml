name: Delete old workflow runs

on:
  schedule:
    - cron: "0 5 * * 0"   # Todos los domingos a las 05:00 UTC
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  cleanup:
    uses: Nivaes/Nivaes.Workflows/.github/workflows/delete-old-runs.yaml@develop  
