namespace Nivaes.App.Cross
{
    // note that this is an exact match for System.Windows.Visibility
    public enum CrossVisibility : byte
    {
        Visible,
        Collapsed,
        Hidden
    }
}
