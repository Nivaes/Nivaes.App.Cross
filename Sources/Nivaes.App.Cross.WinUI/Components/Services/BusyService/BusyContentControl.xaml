<ContentControl
    x:Class="Nivaes.App.Cross.WinUI.BusyContentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <StackPanel Orientation="Vertical" Padding="0" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="Transparent">
        <ProgressRing IsActive="True" Width="64" Height="64" Foreground="{ThemeResource BusyProgressRingBrush}"
                      HorizontalAlignment="Center" Margin="0,20,0,0" />
        <TextBlock x:Name="mMessage" Foreground="{ThemeResource BusyMessageBrush}" 
                   Margin="0,20,0,0"
                   FontSize="25"
                   TextWrapping="WrapWholeWords"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center" />
    </StackPanel>
    
</ContentControl>
