using System;
using System.Collections.Generic;
using System.Text;
using Microsoft.UI.Xaml.Controls;
using Application = Microsoft.UI.Xaml.Application;

namespace Nivaes.App.Cross.WinUI.Components.Helpers
{
    public static class NavigationOrientationHelper
    {
        static void UpdateNavigationViewForElement(bool isLeftMode)
        {
            //NavigationView? _navView = (Application.Current as CrossWinUIApplication)?.MainWindow?.NavigationView;
            //if (isLeftMode)
            //{
            //    _navView?.PaneDisplayMode = NavigationViewPaneDisplayMode.Auto;
            //}
            //else
            //{
            //    _navView?.PaneDisplayMode = NavigationViewPaneDisplayMode.Top;
            //}
        }
    }
}
